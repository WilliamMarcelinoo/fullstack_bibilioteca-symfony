<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Sistema de Livros{% endblock %}</title>
    <!-- Importando Tailwind CSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100">
    <!-- Cabeçalho -->
    <header class="bg-indigo-600 text-white py-4">
      <div class="container mx-auto flex justify-between items-center px-4">
        <h1 class="text-2xl font-bold">
          <a href="{{ path('book_list') }}">Sistema de Livros</a>
        </h1>
        <nav>
          <a href="{{ path('book_list') }}" class="px-3 hover:text-gray-300">Listar</a>
          <a href="{{ path('book_add') }}" class="px-3 hover:text-gray-300">Adicionar</a>
        </nav>
      </div>
    </header>

    <!-- Conteúdo Principal -->
    <main class="container mx-auto mt-8 px-4">
      <!-- Exibição de Mensagens Flash -->
      {% for label, messages in app.flashes %}
        <div class="mb-4 p-4 rounded {{ label == 'success' ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700' }}">
          {% for message in messages %}
            <p>{{ message }}</p>
          {% endfor %}
        </div>
      {% endfor %}

      {% block body %}{% endblock %}
    </main>

    <!-- Rodapé -->
    <footer class="bg-gray-200 text-center py-4 mt-8">
      <p>&copy; {{ "now"|date("Y") }} Sistema de Livros. Todos os direitos reservados.</p>
    </footer>
  </body>
</html>
